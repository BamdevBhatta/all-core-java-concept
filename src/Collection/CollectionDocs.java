package Collection;

public class CollectionDocs {
/*
 * 
 * ======= Java Collection / Collection Framework =======
 *   # use to store and processing dynamic data/ object.
 *   
 *   1> List: Dynamic array.
 *     a> ArrayList (it is slow because arrayList use Stack memory).used for data storage.
 *     b> LinkedList ( is it faster than ArrayList because it uses LinkedList.).,used for data processing.
 *     
 *     
 *   2> Set: Collection of unique data
 *    a> HashSet : Unique data only.
 *    b> TreeSet : Unique + sorted data
 *    c> LinkedHashSet : unique + insertion in order.
 *    
 *   3> Map: Data managed in key value pair.
 *     a> HashMap :  store data in key value pair
 *     b> TreeMap :store data in key value pair + sorted order
 *     c> LinkedHasMap : store data in key value pair + insertion in order
 */
}
